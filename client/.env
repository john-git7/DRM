# Backend API base URL — override for non-local deployments
VITE_API_BASE=http://localhost:5000/api

# Localhost proctoring agent (Phase 3) the player polls before playback
VITE_AGENT_BASE=http://localhost:7891

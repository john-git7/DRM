# DRMShield Agent — no special ProGuard rules required.
# The agent uses only AndroidX + JDK stdlib (java.net.ServerSocket) and reflection-free
# Kotlin, so the defaults are sufficient. Add keep rules here if you introduce
# serialization or reflection-based libraries later.
